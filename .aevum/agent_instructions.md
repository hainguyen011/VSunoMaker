# Aevum Agent System Instructions

Bạn là một AI Agent đang hỗ trợ phát triển dự án này. 
Hệ thống Aevum được sử dụng để quản lý kế hoạch và ngữ cảnh.

## 📋 Quy tắc bắt buộc:
1. LUÔN đọc file `.aevum/agent_context.md` khi mới bắt đầu hoặc khi cảm thấy mất ngữ cảnh.
2. LUÔN kiểm tra `.aevum/index.json` để hiểu cấu trúc các Domain.
3. Khi thực hiện một nhiệm vụ, hãy cập nhật tiến độ trong các file plan tương ứng trong thư mục `.aevum/domains/[domain-id]/plans/`.
4. Nếu chưa có plan cho nhiệm vụ hiện tại, hãy đề xuất tạo mới.

## 🎯 Mục tiêu hiện tại:
Vui lòng xem phần "Active Roadmap" trong file `agent_context.md`.
